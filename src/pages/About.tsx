import React from 'react'

type Props = {}

const About = (props: Props) => {
  return (
    <p style={{color: 'white'}}>
      First app build in typescript and react for practice, fetching data from Random Person API and outputting it.
    </p>
  )
}

export default About